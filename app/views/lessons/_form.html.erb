<%= form_with url: url, model: lesson do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>
  <%= form.label :content %>
  <%= form.text_area :content %>

  <%= form.submit botton_text %>
<% end %>

<% if @lesson.errors.any? %>
  <% @lesson.errors.each do |error|%>
    <%= error.full_message %>
  <% end %>
<% end %>
