<div>
  <h1><%= @lesson.name %></h1>

  <p><%= @lesson.content %></p>
</div>

<div>
  <ul>
    <li><%= link_to 'Editar aula', edit_course_lesson_path(@lesson) %></li>
    <li>
      <%= link_to 'Deletar', course_lesson_path(@lesson), 
                  method: :delete %>
    </li>
    <li><%= link_to 'Voltar', course_path(@course) %></li>
  </ul>
</div>