<h1><%= t '.title' %></h1>

<%= render 'form', course: @course, instructors: @instructors, 
                   button_text: 'Salvar curso' %>

<div>
  <ul>
    <li><%= link_to 'Cancelar', course_path(@course) %></li>
  </ul>
</div>